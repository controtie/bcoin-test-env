{
  "name": "bcoin-test",
  "version": "1.0.0",
  "description": "",
  "main": "connect-to-peer.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "babel-node ./connect-to-peer.js",
    "pool": "babel-node ./connect-to-pools.js",
    "tx": "babel-node ./create-transaction.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0"
  },
  "babel": {
    "presets": [
      "env"
    ]
  }
}

